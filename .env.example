# Public Supabase values may be used client-side when Supabase is added.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only Supabase key. Never expose this client-side.
SUPABASE_SERVICE_ROLE_KEY=

# Put real keys only in .env.local. Do not commit .env.local.
